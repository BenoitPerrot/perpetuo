env = "test"

logging = true

auth {
  jwt {
    secret = "mk#@/ALX*Tq50/qsdf*_$"
  }
}

db {
  ephemeral = true

  name = "Deployment_DB"
  driver = "org.h2.Driver"
  schema = "dbo"
  username = "sa"

  poolMinSize = 1
  poolMaxSize = 5
  idleTimeout = 20000
  connectionTimeout = 30000
}

tokens {
  rundeck = "my-super-secret-token"
}
selfUrl = "http://somewhere"

targetDispatcher {
  type = "singleInvoker"
  singleInvoker {
    type = "dummy"
    dummy {
      name = "test invoker"
    }
  }
}

permissions {
  type = "filterUsernames"
  filterUsernames {
    addProduct = ["bob.the.producer"]
    applyOperation = ["r.eleaser"]
  }
}
