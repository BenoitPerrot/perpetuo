env = "test"

logging = true

db {
  ephemeral = true

  name = "Deployment_DB"
  driver = "org.h2.Driver"
  schema = "dbo"
  username = "sa"

  poolMinSize = 1
  poolMaxSize = 5
  idleTimeout = 20000
  connectionTimeout = 30000
}

targetDispatcher = "com.criteo.perpetuo.dispatchers.CriteoTargetDispatcher"
tokens {
  rundeck = "my-super-secret-token"
}
